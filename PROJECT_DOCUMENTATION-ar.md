# التوثيق المعماري والبرمجي الشامل (بالملّي) لمشروع "متطوع" 🏗️✨

هذا التوثيق مصمم ليكون **المرجع التقني الأدق والأشمل** للمشروع. لا يترك صغيرة ولا كبيرة إلا ويفصلها؛ من رموز الألوان، والأيقونات، وصولاً إلى المتغيرات البيئية والتكوينات (Configurations) الخاصة ببيئة العمل.

---

## 1. الهوية البصرية (Colors, Typography & Styling) 🎨

### أ. لوحة الألوان (Color Palette) دقيقة بالهيكس (Hex Codes):
تم تخصيص ألوان المشروع داخل ملف `tailwind.config.js` لتُغطي كافة الحالات:

*   **الأساسي (Primary - Indigo/Purple)**: 
    *   الطاغي (Main): `#6366f1` (Primary-500)
    *   عند التحويم (Hover): `#4f46e5` (Primary-600) و `#4338ca` (Primary-700)
    *   خلفيات فاتحة للنصوص: `#eef2ff` (Primary-50) و `#e0e7ff` (Primary-100)
    *   التدرج اللوني (Gradient-Primary): `bg-gradient-to-br from-[#6366f1] via-[#4f46e5] to-[#4338ca]`.
*   **النجاح (Success - Emerald)**: 
    *   للقبول ولحالة الفرصة "المتاحة": `#10b981` (500) و `#059669` (600).
*   **التحذير (Warning - Amber)**:
    *   للتنبيهات: `#f59e0b` (500) و `#d97706` (600).
*   **الخطر (Danger - Red)**:
    *   لحذف الفرص أو الرفض: `#ef4444` (500) و `#dc2626` (600) وخلفية الأزرار الرافضة `#fef2f2`.

### ب. الخطوط (Typography):
تم استيراد الخطوط من Google Fonts داخل `globals.css`:
*   `Tajawal`: الخط الأساسي باللغة العربية. تم استيراد الأوزان: `200, 300, 400, 500, 700, 800, 900`.
*   `Inter`: الخط الثانوي (لغة إنجليزية أو أرقام).الأوزان: `300, 400, 500, 600, 700, 800`.
*   تم تطبيق الكود: `font-family: "Tajawal", "sans-serif";` على وسم `body`.

### ج. فئات CSS المخصصة (Custom CSS Classes):
في ملف `globals.css` تم صنع مكونات جاهزة (`@layer components`):
*   `.glass`: زجاجي فاتح `bg-white/70 backdrop-blur-xl border border-white/20 shadow-glass`.
*   `.glass-dark`: زجاجي داكن للوضع الليلي `bg-slate-900/70 backdrop-blur-xl border border-white/10`.
*   `.btn-primary`: زر الفاعل الرئيسي `rounded-xl gradient-primary text-white font-medium shadow-soft`.
*   `.input-field`: حقول الإدخال `border-slate-200 bg-white focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20 outline-none text-right`.

---

## 2. الحركات والتأثيرات (Animations & Framer Motion) 🌀

### أ. Keyframes في Tailwind:
*   `shimmer`: انتقال الخلفية `2s linear infinite` (للتحميل).
*   `float`: تطفو العناصر صعوداً وهبوطاً `20px-` بمدة `6s ease-in-out infinite`.
*   `fade-in`: ظهور تدريجي لـ `0.5s ease-out`.
*   `slide-up`: ظهور من الأسفل `translateY(20px)` إلى مكانه الطبيعي `0`.

### ب. إعدادات مكتبة `Framer Motion` التفصيلية:
استخدمت في المكونات التفاعلية (مثل `OpportunityCard.tsx`):
*   **الدخول (Initial)**: `initial={{ opacity: 0, y: 20 }}`.
*   **فيزيائية القفز (Spring)**: عند ظهور العنصر `transition={{ type: 'spring', stiffness: 200, damping: 20 }}`.
*   **تأثير التحويم (Hover)** على بطاقة الفرصة:
    `whileHover={{ y: -6, boxShadow: '0 20px 50px -12px rgba(0, 0, 0, 0.1)', transition: { type: 'spring', stiffness: 300, damping: 20 } }}`.
*   **تأثير الضغط (Tap)**: عادةً نستخدم `whileTap={{ scale: 0.98 }}`.
*   **تحويم المهارات المستهدفة (Skills Tags)**:
    `whileHover={{ scale: 1.05, backgroundColor: 'rgb(238, 242, 255)' }} transition={{ type: 'spring', stiffness: 400 }}`.

---

## 3. رسائل الإيميلات والإشعارات (Emails exact content & Emojis) ✉️

تم بناء الإيميل باستخدام `HTML Tables` عبر مكتبة `Nodemailer`.
*   **المصدر**: `from: 'متطوع <${process.env.SMTP_EMAIL}>'`.
*   **الرموز التعبيرية (Emojis) المستخدمة برمجياً**:
    *   ترحيب: 🎉
    *   متطوع: 🤝
    *   مؤسسة: 🏢
    *   طلب جديد/تفاصيل: 📋 و 📩 و 👤
    *   موافقة/نجاح: ✅ و 🏆 و 🎊 و 💪
    *   تحديث/رفض: 📝
*   **بنية قالب الإيميل (HTML/CSS)**:
    *   خلفية رمادية فاتحة `#f0f2f5`.
    *   عرض الإيميل `max-width: 560px` داخل حاوية بيضاء بخلفية `border-radius: 16px; box-shadow: 0 2px 16px rgba(0,0,0,0.08)`.
    *   **ألوان رؤوس الرسائل**: متغيرة. للقبول `#ecfdf5` (أخضر)، ولتسجيل الدخول `#eef2ff` (أزرق)، وللطلبات الجديدة `#fefce8` (أصفر).
    *   يحتوي الإيميل على شعار (Logo) يتم جلبه من: `https://mutatawi.vercel.app/logo.png`.
    *   الزر الداخلي (CTA Button): مصمم بكود `padding:14px 36px; border-radius:10px; text-decoration:none; font-weight:700;`.
*   **التنبيهات بداخل المتصفح (Toasts)**:
    *   مُدارة بواسطة مكتبة `react-hot-toast` المغلفة بداخل ملف `ToastProvider.tsx`.

---

## 4. الإعدادات وبيئة التشغيل (Configuration Files) ⚙️

### أ. ملف `next.config.js`:
*   لسماح جلب الصور الخارجية: `domains: ['images.unsplash.com', 'firebasestorage.googleapis.com']`.
*   **إعدادات تجريبية (Experimental)** للسماح بمكتبات السيرفر: `serverComponentsExternalPackages: ['firebase-admin', '@google-cloud/firestore', '@opentelemetry/api']`.
*   **البيئة (Env Forwarding)**: تم توريث كافة مفاتيح `process.env` داخلياً إلى مسارات الـ API لتفادي مشكلة تجاهل Vercel للمفاتيح التي لا تبدأ بـ `NEXT_PUBLIC_`.

### ب. ملف `package.json` والاعتماديات:
*   الأساس: `"next": "^14.2.35"`, `"react": "^18.3.1"`.
*   Firebase: الإدارة `"firebase-admin": "^13.6.1"` والعميل `"firebase": "^12.9.0"`.
*   الإيميل: `"nodemailer": "^8.0.1"`.
*   الأيقونات: `"react-icons": "^5.5.0"` (استخدمنا مكتبة `io5` مثل `IoLocationOutline`, `IoTimeOutline`, `IoCalendarOutline`, `IoPeopleOutline`, `IoTrashOutline`).
*   الحركات: `"framer-motion": "^12.34.0"`.
*   الستايلات: `"tailwindcss": "^3.4.19"`, `"autoprefixer": "^10.4.24"`.

---

## 5. أكواد الـ API المتقدمة (Under the Hood APIs) 🔐

### مسار التقديم (`app/api/applications/apply/route.ts`):
*   التحقق: `adminAuth.verifyIdToken(idToken)`.
*   متغير فريد (Unique Document ID) لتطبيق الفرصة: ```const applicationDocId = `${opportunityId}_${volunteerId}`;``` (يمنع التكرار تماماً).
*   البيانات المحفوظة للطلب: `opportunityId`, `opportunityTitle`, `volunteerId`, `volunteerName`, `volunteerEmail`, `message`, `status: 'pending'`, ورقم هاتف إن وُجد.
*   تحديث الفرص (تسجيل مقعد): `adminDb.collection('opportunities').doc(opportunityId).update({ spotsFilled: FieldValue.increment(1) })`.
*   أسلوب استدعاء الإيميل: **Fire-after-forget** لا يعلق الاستجابة للمستخدم ويستخدم مصفوفة `Promises.allSettled(emailPromises)` لإرسال ايميلين بشكل متزامن.

### مسار إرسال الترحيب (`app/api/auth/first-login/route.ts`):
*   قفل ضد التكرار (Idempotency Lock): يفحص خاصية `userData.firstLoginEmailSent === true`.
*   مباشرة قبل إرسال الإيميل، يتم تحديث قاعدة البيانات: `await userRef.update({ firstLoginEmailSent: true, firstLoginEmailSentAt: new Date() })` لتفادي معضلة تزامن البيانات (Race Conditions).

---

## 6. قاعدة البيانات (Firestore) أسماء وبنية 🗄️
*   **المجموعات (Collections)**:
    1.  `users`
    2.  `opportunities`
    3.  `applications`
*   **حقول نظامية**: يتم وضع الطوابع الزمنية عبر `FieldValue.serverTimestamp()` للحفاظ على التوقيت الفعلي من خوادم جوجل.
*   **أنواع الحسابات (`role`)**: دائمًا مابين `volunteer` (متطوع) أو `organization` (منظمة).

---

هذا المرجع يُمثل البنية التحتية الدقيقة، وكل كود لون، وحركة، ونوع ملف وحزمة، وشيفرة استُعملت في بناء هذا الصرح التقني العظيم المسمى "متطوع".
