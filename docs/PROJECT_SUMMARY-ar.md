# توثيق مشروع "متطوع" (Mutatawi)

هذا الملف يحتوي على ملخص شامل وتوثيق لكل المراحل التي مر بها تطوير الموقع منذ بداية كتابة الكود وحتى الآن، بالإضافة إلى توضيح للتقنيات المستخدمة وهيكلية النظام.

---

## 1. التقنيات الأساسية (Tech Stack) 🛠️
تم بناء المشروع باستخدام أحدث تقنيات الويب لضمان الأداء العالي والتفاعلية:
*   **إطار العمل الأساسي**: Next.js (الإصدار 14، بنظام App Router المستحدث).
*   **الواجهات الأمامية**: React (الإصدار 18).
*   **التصميم والتنسيق**: Tailwind CSS (لتسهيل وسرعة بناء واجهات متجاوبة).
*   **الحركات المتقدمة (Animations)**: Framer Motion (لإضافة حركات انتقالية سلسة واحترافية).
*   **الخلفية وقاعدة البيانات (Backend & DB)**: Firebase (Firestore, Authentication, Storage), و Firebase Admin.
*   **نظام البريد الإلكتروني**: Nodemailer مع Gmail SMTP (لإرسال إشعارات المنصة).
*   **لغة البرمجة**: TypeScript (لأمان التكويد ومنع الأخطاء البرمجية).

---

## 2. التسلسل الزمني للتطوير والإنجازات (Development History & Milestones) 📅

### المرحلة الأولى: التأسيس والبنية التحتية 🏗️
*   **إنشاء المشروع**: بناء الهيكلية الأساسية للمشروع باستخدام Next.js وتكوين `tailwind.config.ts` و `next.config.js`.
*   **ربط الواجهة بالخلفية**: الإعداد الأولي وتوصيل منصة Firebase بالمشروع لدعم قواعد البيانات والمصادقة.

### المرحلة الثانية: تصميم واجهة احترافية (Premium UI Overhaul) 🎨
*   **تحسين التصميم البصري**: تطوير الواجهات لتصبح فاخرة واحترافية (Premium Flow). الاعتماد على الشفافية وتأثير الزجاج المضبب (Glassmorphism)، تدرجات الألوان العصرية (Gradients)، وجعل التجربة البصرية مبهرة.
*   **حركات انتقالية ناعمة (Smooth Animations)**: إضافة تحسينات عميقة على حركات أزرار الواجهة والنصوص لتصبح مرنة وأسرع بالاعتماد على فيزياء الحركة المتقدمة (Spring Physics) عبر Framer Motion.
*   **التحديثات شملت**: شاشات الانتظار (Splash)، تسجيل الدخول (Login)، الصفحة الرئيسية (Home)، الملف الشخصي (Profile)، وشريط التصفح السفلي (Bottom Navigation).

### المرحلة الثالثة: نظام إدارة فرص التطوع 🤝
*   **إنشاء وإدارة الفرص**: تمكين حسابات "المؤسسات" من نشر الفرص التطوعية.
*   **تطوير نظام رفع الصور**: حل المشكلات وتصحيح الكود لتمكين المؤسسات من رفع صور مصاحبة لكل مشروع تطوعي بنجاح إلى (Firebase Storage).
*   **التقديم السريع للمتطوع (One-Click Application)**: تم تبسيط تجربة المستخدم بشكل كبير؛ بدلا من وجود نافذة تتطلب إدخال رقم هاتف أو رسالة نصية طويلة، تم تغيير التطبيق ليسمح بالتقديم الفوري بضغطة زر واحدة من صفحة الفرصة مباشرة.
*   **إدارة الطلبات**: توثيق وبناء أكواد للتعامل مع معالجة وإلغاء الفرص (Withdraw System).

### المرحلة الرابع: نظام الإشعارات والبريد (Email & Notifications) ✉️
*   **نظام التنبيهات المنبثقة**: استخدام مكتبة `react-hot-toast` للتجاوب مع عمليات المستخدم وإعطائه ردود فعل سريعة لأي إجراء، مثل النجاح في التسجيل أو الفشل وغيرها.
*   **ربط الـ Gmail**: تم برمجة إعدادات المراسلة وتحويل النظام من استخدام (Resend) إلى (Nodemailer - Gmail SMTP) بعد تنقيح وإصلاح أخطاء الربط (Authentication/Configuration Issues). 
*   **رسائل تفاعلية**: المنصة مُجهزة الآن لإرسال بريد إلكتروني للمستخدم فور إكماله التسجيل، أو عندما يقدم متطوع طلب على فرصة ما كإشعار للمؤسسة/المتطوع المعني.

### المرحلة الخامسة: إصلاحات برمجية دقيقة (Bug Fixes & Refactoring) 🐛
تم العمل على تصحيح سلسلة من المشكلات المتقدمة لضمان استقرار التطبيق وعمله الخالي من الأخطاء:
*   إصلاح أخطاء وقت البناء من Next.js (تحديداً `EINVAL error` الخاصة ببيئة نظام ويندوز).
*   التخلص من تكرار المسارات (Duplicate Page Paths) في نظام المواجهة (App Router).
*   إصلاح مشاكل التصدير (`ES Module Warning`/`type: module`) لملفات التكوين.
*   إصلاح مشكلة التنبيهات من TypeScript بخصوص أنواع حركة الـ Variables (مثل مشكلة الـ Easing properties).
*   تحسين سريع في الأداء (Performance Optimization)، لتجنب أي تشنج (Lag) وجعل الموقع أكثر سرعة وخفة.

### المرحلة السادسة: التأمين ورفع النسخ الاحتياطية (Version Control) ☁️
*   خطوة رفع كامل مجلدات المشروع (بعد تصفية الملفات الخاصة مثل البيئة المحلية) وتوثيق التغييرات على منصة (GitHub) لتأمين الأكواد وضمان توافرها كنسخة للمتابعة.

---

## 3. هندسة المشروع وأهم الملفات (Project Core Structure) 📁
يتكون المشروع من فروع أساسية لتسهيل الصيانة وتقليل التعقيدات:
*   **`/app/(auth)`**: الشاشات الخاصة بالتسجيل، وتسجيل الدخول، واستكمال بيانات تسجيل الدخول لأول مرة.
*   **`/app/(dashboard)`**: اللوحات الإدارية والشخصية للمستخدمين للتحكم بحساباتهم.
*   **`/app/api/`**: الواجهة البرمجية للنظام ومسارات الخوادم الوسيطة (Server Actions/Routes). من ضمنها مسارات `applications`, `auth`, و `send-email`.
*   **`/app/components/`**: مكتبة ضخمة من واجهات الاستخدام الجاهزة (UI، أزرار، مربعات نصوص، وتنسيقات موحدة قابلة لإعادة الاستخدام).
*   **`/app/lib/`**: ملفات العمليات المساعدة كربط قواعد البيانات، وخدمات إرسال الإيميل (مثال: `email.ts`).

---

هذه الوثيقة تُمثل ملخصاً لكل ما جرى إنجازه في بناء منصة "متطوع" منذ السطر الأول، مع تقديم أكواد حديثة، بنية برمجية سليمة، وواجهات فخمة وعالية الأداء.
