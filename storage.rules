rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // ===================== COURSE THUMBNAILS =====================
    // المستخدمين المسجلين فقط يقدرون يرفعون صور الكورسات
    // الحد الأقصى 10MB + نوع الملف لازم يكون صورة
    match /course-thumbnails/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }

    // ===================== GENERATED FILES =====================
    // الملفات المولدة من الذكاء الاصطناعي (مثل تقارير أو عروض تقديمية)
    match /generated/{fileName} {
      allow read: if true;
      allow write: if false; // الرفع مسموح فقط من السيرفر (Admin SDK)
    }

    // ===================== COURSE SLIDES =====================
    // السماح برفع ملفات عروض PowerPoint التقديمية
    match /course-slides/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 50 * 1024 * 1024;
    }

    // ===================== COURSE VIDEOS =====================
    // السماح برفع ملفات الفيديو
    match /course-videos/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 500 * 1024 * 1024;
    }

    // ===================== DEFAULT: DENY ALL =====================
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
